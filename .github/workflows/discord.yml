name: Notify Discord on Release

on:
  release:
    types: [published]

  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (optional)'
        required: false

jobs:
  notify-discord:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare release data
        id: release
        env:
          TAG: ${{ github.event.inputs.tag }}
        run: |
          if [ -n "$TAG" ]; then
            RELEASE_JSON=$(curl -s \
              -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              https://api.github.com/repos/${{ github.repository }}/releases/tags/$TAG)

            echo "name=$(echo $RELEASE_JSON | jq -r '.name')" >> $GITHUB_OUTPUT
            echo "body<<EOF" >> $GITHUB_OUTPUT
            echo "$(echo $RELEASE_JSON | jq -r '.body')" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "url=$(echo $RELEASE_JSON | jq -r '.html_url')" >> $GITHUB_OUTPUT
            echo "tag=$(echo $RELEASE_JSON | jq -r '.tag_name')" >> $GITHUB_OUTPUT
          else
            echo "name=${{ github.event.release.name }}" >> $GITHUB_OUTPUT
            echo "body<<EOF" >> $GITHUB_OUTPUT
            echo "${{ github.event.release.body }}" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            echo "url=${{ github.event.release.html_url }}" >> $GITHUB_OUTPUT
            echo "tag=${{ github.event.release.tag_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Send Discord Notification
        env:
          DISCORD_WEBHOOK: ${{ secrets.USFDISCORDHOOK }}
          RELEASE_NAME: ${{ steps.release.outputs.name }}
          RELEASE_BODY: ${{ steps.release.outputs.body }}
          RELEASE_URL: ${{ steps.release.outputs.url }}
          RELEASE_TAG: ${{ steps.release.outputs.tag }}

        run: |
          # --- 1. Очистка markdown (видаляємо badges/зображення) ---
          CLEAN_DESCRIPTION=$(echo "$RELEASE_BODY" \
            | sed -E 's/\[!\[.*?\]\(.*?\)\]\(.*?\)//g' \
            | sed -E 's/!\[.*?\]\(.*?\)//g')

          # --- 2. Вставка лінків на місце (після "Насолоджуйтесь...") ---
          CLEAN_DESCRIPTION=$(echo "$CLEAN_DESCRIPTION" | sed '/Насолоджуйтесь/ a\
          \[▶️ Демонстраційне відео](https://youtu.be/4AwL8TKXcTU)\
          \[🇺🇦 Україїнізатор для Star Citizen](https://scloc.pp.ua/)\
          \[💬 Приєднатись до найбільшої україномовної спільноти](https://discord.gg/y2c7M9cgbk)\')

          # --- 3. Нормалізація переносів ---
          CLEAN_DESCRIPTION=$(echo "$CLEAN_DESCRIPTION" \
            | sed -E ':a;N;$!ba;s/\n{3,}/\n\n/g')

          # --- 4. Прибираємо пусті рядки в кінці + обрізка ---
          FORMATTED=$(echo "$CLEAN_DESCRIPTION" \
            | sed -E ':a;/^\n*$/{$d;N;ba}' \
            | head -c 3800)

          # --- 5. Embed ---
          EMBED=$(jq -n \
            --arg title "🚀 Новий реліз: $RELEASE_NAME" \
            --arg url "$RELEASE_URL" \
            --arg version "Версія: $RELEASE_TAG" \
            --arg body "$FORMATTED" \
            --argjson color 5814783 \
            '{
              embeds: [{
                title: $title,
                url: $url,
                description: ($version + "\n\n" + $body),
                color: $color,
                thumbnail: {
                  url: "https://img.youtube.com/vi/4AwL8TKXcTU/hqdefault.jpg"
                }
              }]
            }')

          # --- 6. Відправка ---
          curl -X POST \
            -H "Content-Type: application/json" \
            -d "$EMBED" \
            "$DISCORD_WEBHOOK"
